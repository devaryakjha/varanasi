import 'package:flutter/material.dart';
import 'package:varanasi_mobile_app/utils/keys.dart';

/// The build context of the app,
/// extracted from the root navigator key.
BuildContext get appContext => rootNavigatorKey.currentContext!;
